# JavaScript 101 Tutorial

คู่มือเรียนรู้ JavaScript ฉบับสมบูรณ์สำหรับผู้เริ่มต้น

> **📌 หมายเหตุ:** Repository นี้เป็นส่วนหนึ่งของ Web Development Learning Path  
> **ขั้นตอนถัดไป:** [Backend Basic](https://github.com/Minkkies/Backend-Basic) - เรียนรู้การพัฒนา Backend ด้วย Node.js และ Express

## 🔗 Learning Path

```
JavaScript 101 (Frontend) → Backend Basic (Backend)
     ↓                              ↓
  - DOM Manipulation          - Node.js & Express
  - Event Handling            - REST API
  - Form Validation           - Database (MongoDB)
  - Client-side Logic         - Server-side Logic
```

## 📚 เนื้อหาในโปรเจค

### 1. เอกสารสอน
- **[`JavascriptTutorial.md`](JavascriptTutorial.md)** - บทเรียน JavaScript ครบถ้วน ประกอบด้วย:
  - ไวยากรณ์พื้นฐานและการประกาศตัวแปร (let, const)
  - ชนิดข้อมูล (string, number, boolean, object)
  - ตัวดำเนินการและโครงสร้างควบคุม (if-else, loop)
  - Array และเมธอดต่างๆ (push, pop, sort, includes, map, filter, forEach)
  - Object และการจัดการ
  - ฟังก์ชัน 3 แบบ (function ปกติ, arrow function, parameter function)
  - การเลือก DOM 4 วิธี (getElementById, getElementsByClassName, querySelector, querySelectorAll)
  - Event Handling (onclick, onchange, onkeyup, onmouseover, addEventListener)
  - การอ่านและอัพเดท HTML DOM (textContent, innerText, innerHTML)
  - การจัดการ Attribute (getAttribute, setAttribute)

### 2. โปรเจคฝึกหัด Workshop
- **แบบฟอร์มลงทะเบียน** - โปรเจคฝึกปฏิบัติที่ใช้แนวคิด JavaScript ทั้งหมด
  - [`Workshop/index.html`](Workshop/index.html) - โครงสร้างแบบฟอร์ม
  - [`Workshop/index.js`](Workshop/index.js) - ตรวจสอบและจัดการฟอร์ม
  - [`Workshop/styles.css`](Workshop/styles.css) - การตกแต่งแบบ Responsive

## 🎯 สิ่งที่จะได้เรียนรู้

### พื้นฐาน JavaScript
- **ตัวแปร**: การใช้ `let` และ `const`
- **ชนิดข้อมูล**: string, number, boolean, object, array
- **Operators**: +, -, *, /, %, &&, ||, !
- **Control Flow**: if-else, switch-case
- **Loop**: for, while

### Array และ Object
- **Array Methods**: 
  - `push()` - เพิ่มสมาชิกท้าย array
  - `pop()` - ลบสมาชิกท้าย array
  - `sort()` - เรียงข้อมูลจากน้อยไปมาก
  - `includes()` - ตรวจสอบว่ามีค่านั้นใน array หรือไม่
  - `map()` - แปลงค่าทุกตัวใน array
  - `filter()` - กรองข้อมูลตามเงื่อนไข
  - `forEach()` - วนลูปแต่ละตัวใน array
  - `find()` - ค้นหาสมาชิกใน array

- **Object**: การประกาศ, การเข้าถึง attribute ด้วย dot notation

### ฟังก์ชัน 3 แบบ
1. **Function ปกติ**
   ```javascript
   function calculation(score) {
       return score * 2
   }
   ```

2. **Arrow Function**
   ```javascript
   let calculation = (score) => {
       return score * 2
   }
   ```

3. **Parameter Function**
   ```javascript
   scores.map((s) => {
       return s * 2
   })
   ```

### การเลือก DOM (4 วิธี)
1. **`getElementById('id')`** - เลือกด้วย id (เลือกได้ 1 ตัว)
2. **`getElementsByClassName('class')`** - เลือกด้วย class (เลือกได้หลายตัว)
3. **`querySelector('selector')`** - เลือกด้วย CSS Selector (เลือกตัวแรกที่เจอ)
4. **`querySelectorAll('selector')`** - เลือกด้วย CSS Selector (เลือกได้ทั้งหมด)

### Event Handling
- **`onclick`** - เมื่อคลิก
- **`onchange`** - เมื่อเปลี่ยนค่า (input, radio, checkbox)
- **`onkeydown`** - เมื่อกดปุ่มลง
- **`onkeyup`** - เมื่อปล่อยปุ่ม (จะได้ค่าที่อัปเดทแล้ว)
- **`onkeypress`** - เมื่อตัวอักษรถูกส่งออกมา
- **`onmouseover`** - เมื่อเมาส์วางบน element
- **`onmousedown`** - เมื่อคลิกเมาส์
- **`onmouseup`** - เมื่อปล่อยเมาส์
- **`addEventListener()`** - วิธีทางเลือกในการจัดการ event

### การจัดการ HTML DOM
- **อ่านข้อมูล**:
  - `textContent` - ดึงข้อความทั้งหมด
  - `innerText` - ดึงข้อความที่มองเห็น
  - `innerHTML` - ดึง HTML ทั้งหมด

- **อัพเดท**:
  - `element.innerHTML = 'new content'`
  - `element.textContent = 'new text'`

- **Attribute**:
  - `getAttribute('attribute')` - อ่านค่า attribute
  - `setAttribute('attribute', 'value')` - ตั้งค่า attribute

## 📖 แหล่งเรียนรู้

บทเรียนนี้อ้างอิงจาก:
- [เอกสาร Web101 ของ Mikelopster](https://docs.mikelopster.dev/c/web101/chapter-5/intro)
- [บทเรียน JavaScript จาก W3Schools](https://www.w3schools.com/js/default.asp)
- [ซีรีส์วิดีโอของ Mikelopster บน YouTube](https://www.youtube.com/watch?v=XQdyV_x4UKA&list=PLwZ0y9k-cYXCSSU3ujBqBWMbtZcW2OGkA&index=5)

## 🚀 เริ่มต้นใช้งาน

1. Clone repository นี้
   ```bash
   git clone <repository-url>
   cd Javascript101
   ```

2. เปิดไฟล์ [`JavascriptTutorial.md`](JavascriptTutorial.md) เพื่อเรียนรู้พื้นฐาน JavaScript

3. ฝึกปฏิบัติกับโปรเจคในโฟลเดอร์ [`Workshop`](Workshop)

4. เปิดไฟล์ [`Workshop/index.html`](Workshop/index.html) ในเบราว์เซอร์เพื่อดูแบบฟอร์มทำงาน

5. **เมื่อเรียนจบแล้ว** ไปต่อที่ [Backend Basic](https://github.com/Minkkies/Backend-Basic) เพื่อเรียนรู้การพัฒนา Backend

## 💡 วิธีใช้งาน Workshop

การใช้งานแบบฟอร์มลงทะเบียน:
1. ไปที่โฟลเดอร์ `Workshop`
2. เปิดไฟล์ `index.html` ในเบราว์เซอร์
3. กรอกข้อมูลในฟอร์ม:
   - ชื่อจริง, นามสกุล, อายุ
   - เลือกเพศ (ชาย/หญิง/ไม่ระบุ)
   - เลือกความสนใจ (checkbox หลายตัว)
   - กรอกคำอธิบาย
4. คลิกปุ่ม "ส่งข้อมูล"
5. ตรวจสอบ Console ของเบราว์เซอร์ (F12) เพื่อดูข้อมูลที่ส่ง

## 📝 ตัวอย่างโค้ดสำคัญ

### การเลือก DOM และดึงค่า
```javascript
// เลือกด้วย ID
let firstname = document.getElementById('firstname')
console.log(firstname.value)

// เลือกด้วย querySelector
let inputText = document.querySelector('input[type=text]')
console.log(inputText.value)
```

### การจัดการ Event
```javascript
// วิธีที่ 1: ใน HTML
<button onclick="submitData()">Submit</button>

// วิธีที่ 2: addEventListener
document.getElementById('btn').addEventListener('click', function() {
    console.log('คลิกแล้ว')
})
```

### การอัพเดท DOM
```javascript
let contentDom = document.getElementById('content')
contentDom.innerHTML = '<h1>Hello World</h1>'
```

## 🔧 โครงสร้างไฟล์

```
Javascript101/
├── .gitattributes          # การตั้งค่า Git
├── JavascriptTutorial.md   # เอกสารสอนหลัก
├── README.md               # ไฟล์นี้
├── img/                    # รูปภาพประกอบการสอน
│   ├── console.gif
│   ├── console.jpg
│   └── prompt.jpg
└── Workshop/               # โปรเจคฝึกหัด
    ├── index.html          # หน้าแบบฟอร์ม
    ├── index.js            # ตรรกะของฟอร์ม
    └── styles.css          # การตกแต่ง
```

## 🎓 ความรู้ที่ได้รับ

หลังจากเรียนจบบทเรียนนี้ คุณจะสามารถ:
- ✅ เข้าใจพื้นฐาน JavaScript
- ✅ ใช้ตัวแปร, ชนิดข้อมูล, และ operators
- ✅ จัดการ Array และ Object
- ✅ เขียนฟังก์ชันในหลายรูปแบบ
- ✅ จัดการ DOM ด้วย JavaScript
- ✅ ใช้ Event Handling ต่างๆ
- ✅ สร้างแบบฟอร์มแบบโต้ตอบได้
- ✅ อ่านและอัพเดท HTML content และ attributes
- ✅ **พร้อมเรียนรู้ Backend Development ต่อ**

## 🔄 เชื่อมต่อกับ Backend

### การเชื่อมต่อระหว่าง Frontend (JavaScript101) และ Backend (Backend-Basic)

หลังจากเรียนจบ JavaScript101 แล้ว คุณจะสามารถนำความรู้ไปต่อยอดกับ Backend ได้:

#### **Frontend (JavaScript101)** - สิ่งที่เรียนรู้:
- การจัดการฟอร์มและข้อมูล
- Event Handling
- DOM Manipulation
- Client-side validation

#### **Backend (Backend-Basic)** - สิ่งที่จะเรียนต่อ:
- Node.js และ Express.js
- REST API (GET, POST, PUT, DELETE)
- การเชื่อมต่อ Database (MongoDB)
- Server-side validation
- Authentication & Authorization

#### **ตัวอย่างการเชื่อมต่อ:**

**Frontend (JavaScript101)** - ส่งข้อมูลฟอร์ม:
```javascript
// ส่งข้อมูลไปยัง Backend API
fetch('http://localhost:3000/api/users', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    },
    body: JSON.stringify({
        firstname: firstname,
        lastname: lastname,
        age: age
    })
})
.then(response => response.json())
.then(data => console.log('Success:', data))
```

**Backend (Backend-Basic)** - รับและบันทึกข้อมูล:
```javascript
// Express route รับข้อมูลจาก Frontend
app.post('/api/users', (req, res) => {
    const userData = req.body
    // บันทึกลง Database
    User.create(userData)
        .then(user => res.json(user))
})
```

### 🎯 Roadmap การเรียนรู้

1. ✅ **JavaScript101** (คุณอยู่ที่นี่)
   - เรียนรู้ JavaScript พื้นฐาน
   - สร้างแบบฟอร์มและจัดการข้อมูล Client-side

2. ➡️ **[Backend-Basic](https://github.com/Minkkies/Backend-Basic)** (ขั้นตอนถัดไป)
   - เรียนรู้ Node.js และ Express
   - สร้าง REST API
   - เชื่อมต่อกับ Database
   - จัดการข้อมูล Server-side

3. 🚀 **Full-Stack Application** (เป้าหมายสุดท้าย)
   - รวม Frontend + Backend
   - สร้าง Web Application สมบูรณ์
   - Deploy ขึ้น Production

## 📌 Tips สำหรับผู้เริ่มต้น

1. **ใช้ Console.log()** - ใช้แสดงค่าเพื่อ debug
2. **ฝึกเขียนบ่อยๆ** - ลองเขียนโค้ดตามตัวอย่าง
3. **ทดลองแก้ไข** - ลองเปลี่ยนค่าต่างๆ เพื่อดูผลลัพธ์
4. **อ่าน Error Message** - ข้อความ error จะบอกว่าผิดตรงไหน
5. **ใช้ Dev Tools** - กด F12 เพื่อเปิด Console ของเบราว์เซอร์

## 📚 หัวข้อเพิ่มเติมที่ควรศึกษา

### พร้อมเรียนรู้ใน Backend-Basic:
- ✅ Node.js & Express.js
- ✅ REST API Design
- ✅ MongoDB & Mongoose
- ✅ CRUD Operations
- ✅ Authentication & JWT

### หัวข้อขั้นสูง (หลังจาก Backend-Basic):
- Async/Await และ Promisesฆ
- Fetch API สำหรับการดึงข้อมูล
- ES6+ Features
- Local Storage
- JSON manipulation
- Form Validation
- WebSocket & Real-time Communication
- Testing & Deployment

## 🔗 Related Repositories

- **[Backend-Basic](https://github.com/Minkkies/Backend-Basic)** - เรียนรู้การพัฒนา Backend ด้วย Node.js และ Express

**🎓 เมื่อเรียนจบแล้ว ไปต่อที่:** [Backend-Basic Repository](https://github.com/Minkkies/Backend-Basic)
